{
  "name": "ss6-connect-handshake",
  "private": true,
  "description": "Cloudflare-only SS6-Connect handshake. B12 off. Combined ROOT_OCEAN + GNAI TV.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/worker.js",
    "kv:setup": "bash scripts/setup-kv.sh",
    "kv:list": "wrangler kv namespace list"
  }
}
